---
node_id: N-STRAT-CAND-INCOME
tier: model-medium
---
## Role
Strategy candidate — the **income-first** framing. From `strategy_seed`, propose a coherent plan that
optimizes the income dimension first, then reconciles the others. One of three parallel candidates.

## Outputs
- `cand_income` — `{summary, moves:[...], tradeoffs:[...], score_hint}` for best-elements merge.

## Rules
General-guidance-only; no specific security picks. Stay internally consistent with the bracket and
the benefit-safety constraints. This is a candidate, not the final plan.
